<Window x:Class="ChopshopSignin.PruneDialog"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Prune File" Width="150"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <DatePicker Grid.Row="0" Grid.ColumnSpan="2" x:Name="PruneDate"/>
        <Button Grid.Row="1" Grid.Column="0" Margin="5" Click="OK_Click">OK</Button>
        <Button Grid.Row="1" Grid.Column="1" Margin="5" Click="Cancel_Click">Cancel</Button>
    </Grid>
</Window>
